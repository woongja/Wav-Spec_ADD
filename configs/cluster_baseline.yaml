# Baseline Configuration: Pure Cluster-based Learning
# Uses only Bonafide-cluster metric loss (no CE)

model:
  name: ssl_cluster_model
  emb_size: 256
  pooling_type: mean  # Options: mean, max, attention
  ssl_model_path: /home/woongjae/wildspoof/xlsr2_300m.pt

data:
  name: data_utils
