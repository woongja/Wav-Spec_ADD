# CE Baseline Configuration: Traditional Cross-Entropy Loss
# For comparison with cluster-based approaches

model:
  name: ssl_cluster_model
  emb_size: 256
  pooling_type: mean  # Options: mean, max, attention
  ssl_model_path: /home/woongjae/wildspoof/xlsr2_300m.pt

data:
  name: data_utils
